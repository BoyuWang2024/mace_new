# FGE 结果转换工具（内部使用）

本目录只用于把已经验证的旧 FGE 模型与成员级预测转换成当前正式结果格式，不属于发布内容。

转换器只复制 raw/EMA 最终成员模型，规范化既有预测张量，并调用正式评估与验证模块重新计算汇总结果。它不会恢复 checkpoint、训练模型或执行预测，也不会迁移日志、W&B 文件、checkpoint、诊断文件和图片。

旧来源路径和逐文件迁移审计只写入正式结果目录之外的内部审计文件；正式结果中不包含旧来源、迁移标记或数据路径/身份信息。

